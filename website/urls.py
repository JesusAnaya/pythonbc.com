from django.conf.urls import patterns, url
#from website.views import FeedView


urlpatterns = patterns('',
#    url(r"^blog/feeds/(?P<format>.*)/$", FeedView.as_view(),
#       name='pythonbc_feeds'),
)
